apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: no-quota-compat-pvc
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
