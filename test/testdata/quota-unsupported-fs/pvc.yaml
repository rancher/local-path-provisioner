apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-path-pvc
spec:
  storageClassName: local-path-quota-xfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
