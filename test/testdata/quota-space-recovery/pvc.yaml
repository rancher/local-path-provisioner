apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quota-recovery-pvc
spec:
  storageClassName: quota-recovery-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
