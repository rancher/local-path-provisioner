apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quota-resize-pvc
spec:
  storageClassName: quota-resize-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
