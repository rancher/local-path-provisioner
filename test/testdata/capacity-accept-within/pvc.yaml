apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-path-pvc
spec:
  storageClassName: local-path-capacity-limited
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
