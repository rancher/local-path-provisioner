apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quota-shrink-ok-pvc
spec:
  storageClassName: quota-shrink-ok-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
