apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-path-pvc
spec:
  storageClassName: local-path-size-limited
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
