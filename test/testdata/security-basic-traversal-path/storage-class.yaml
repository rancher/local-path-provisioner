apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: security-test-storage
provisioner: rancher.io/local-path
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Delete
parameters:
  pathPattern: "{{ .PVC.Name }}/../../../etc/passwd"
