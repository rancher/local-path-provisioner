apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: security-test-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: security-test-storage
  resources:
    requests:
      storage: 1Gi
