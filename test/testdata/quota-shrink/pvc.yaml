apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quota-shrink-pvc
spec:
  storageClassName: quota-shrink-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
